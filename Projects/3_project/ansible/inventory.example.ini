[cloudops]
# Documentation-only address. Replace it with the public IP from Terraform.
cloudops-lab ansible_host=203.0.113.20 ansible_user=ubuntu

[cloudops:vars]
ansible_python_interpreter=/usr/bin/python3
monitoring_allowed_cidr=203.0.113.10/32

# Supply a real key at runtime instead of committing it here:
# ansible_ssh_private_key_file=~/.ssh/replace-me
