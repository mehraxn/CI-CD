# Keep shell scripts and Dockerfiles LF-only so they run on Linux servers
# regardless of the contributor's OS / core.autocrlf setting.
*.sh        text eol=lf
Dockerfile  text eol=lf
*.conf      text eol=lf
