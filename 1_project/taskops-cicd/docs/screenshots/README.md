# Screenshots

| File | What it shows | How it was captured |
|------|---------------|---------------------|
| `tasks.png` | The task dashboard (`/tasks`) with pending and completed tasks and the live health status bar. | Rendered from the actual running application with seeded sample tasks. |

## Suggested additional captures (after the repo is on GitHub)

- `ci-success.png` — the Actions tab showing a green CI run (quality gates + image scan + smoke test).
- `cd-deploy.png` — a green CD run ending in the post-deploy health check.

Capture these from your own repository's Actions tab once the pipeline has run;
they are intentionally not faked here.
